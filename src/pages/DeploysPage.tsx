import PagePlaceholder from "../components/PagePlaceholder";

export default function DeploysPage() {
  return (
    <PagePlaceholder
      title="Deploys"
      description="Deploy history and live per-instance rollout convergence. Backed by GET /mgmt/deploys."
    />
  );
}
