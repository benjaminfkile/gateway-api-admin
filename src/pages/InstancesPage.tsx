import PagePlaceholder from "../components/PagePlaceholder";

export default function InstancesPage() {
  return (
    <PagePlaceholder
      title="Instances"
      description="Fleet list with heartbeats, gateway versions, and leader badge. Backed by GET /mgmt/instances."
    />
  );
}
