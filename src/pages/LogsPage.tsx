import PagePlaceholder from "../components/PagePlaceholder";

export default function LogsPage() {
  return (
    <PagePlaceholder
      title="Logs"
      description="xterm console panel with service and instance pickers. Backed by GET /mgmt/services/{name}/logs."
    />
  );
}
