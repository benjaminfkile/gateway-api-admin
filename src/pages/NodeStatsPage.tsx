import PagePlaceholder from "../components/PagePlaceholder";

export default function NodeStatsPage() {
  return (
    <PagePlaceholder
      title="Node stats"
      description="Host disk, memory, and Docker usage per instance, plus gateway build info."
    />
  );
}
