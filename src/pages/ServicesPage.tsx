import PagePlaceholder from "../components/PagePlaceholder";

export default function ServicesPage() {
  return (
    <PagePlaceholder
      title="Services"
      description="Fleet grid: status, digest, and uptime per service with start / stop / restart / deploy / rollback actions. Backed by GET /mgmt/services."
    />
  );
}
